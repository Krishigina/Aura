package com.aura.core.presentation.mvi

/** Marker for all MVI UI state models. */
interface UiState

/** Marker for all MVI user/system intents. */
interface Intent

/** Marker for one-off side effects (navigation, toasts, etc). */
interface Effect
